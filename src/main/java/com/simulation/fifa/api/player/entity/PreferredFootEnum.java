package com.simulation.fifa.api.player.entity;

import lombok.Getter;

import java.util.Arrays;

@Getter
public enum PreferredFootEnum {
    LEFT("LEFT"),
    RIGHT("RIGHT");

    PreferredFootEnum(String foot) {
    }
}
